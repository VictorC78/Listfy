import 'package:flutter/material.dart';

class PastasPage extends StatelessWidget {
  const PastasPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
